# Инструкция для Trinity Large Preview (TLP)
## Цель: Реализация Трека №16 (Proactive Brain)

Вы — Principal AI Architect. Ваша задача — создать "разум" бота, который будет принимать решения на основе зрения и слуха.

### Протокол действий:
1. **Глубокий анализ:**
   - Прочитайте ВСЕ завершенные треки (статус `[x]`) в `conductor/tracks.md`.
   - Изучите, как работают модули Vision (T13), Audio (T14) и Memory (T15). Вы — дирижер, который будет ими управлять.

2. **Статус:**
   - Установите статус **Track 16** в `[~]`.

3. **Исполнение (Reasoning):**
   - Реализуйте в `core/brain.py` логику "внутреннего желания" говорить.
   - Используйте асинхронность (`asyncio`), чтобы бот мог одновременно "думать" и "слушать".
   - Интегрируйте вызовы моделей через API-бридж, учитывая `temperature` и `sarcasm_level` из конфига.

4. **Синхронизация:**
   - Это критический этап. По завершении вы ДОЛЖНЫ проверить соответствие `conductor/product.md`. Если "мозг" умеет больше или меньше заявленного — предложите правку в `product.md`.

5. **Финализация:**
   - Установите статус `[x]`.
   - Коммит: `feat(brain): Implement proactive reasoning logic`.
