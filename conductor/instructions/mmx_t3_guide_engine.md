# Инструкция для Minimax M2.5 (MMX): Трек №3
## Цель: Реализация Guide System Engine (Движок подсказок)

Вы — Lead Backend Developer. Ваша задача — создать "сердце" системы интерактивной помощи, которое управляет показом подсказок в UI.

### Протокол действий:
1. **Инициализация:**
   - Прочитайте `conductor/tracks.md`. Найдите **Track 3: Guide System Engine**.
   - Немедленно обновите статус трека с `[ ]` на `[~]`.
   - Изучите `ui/main_window.py` и `core/config.py`.

2. **Анализ задачи:**
   - Перейдите в `conductor/tracks/guide_engine_20250225/`.
   - Изучите `spec.md` и `plan.md`.

3. **Исполнение:**
   - Реализуйте `core/guide_manager.py` — центральный контроллер подсказок.
   - Интегрируйте его с `MainWindow`, чтобы подсказки могли появляться поверх интерфейса или в специальных виджетах.
   - Реализуйте логику "триггеров" (когда показывать подсказку).

4. **Завершение:**
   - Проверьте работоспособность (хотя бы через логи или мок-тесты).
   - Обновите статус трека в `tracks.md` на `[x]`.
   - Выполните коммит: `git add . && git commit -m "feat(guides): Implement core Guide System Engine"`.
