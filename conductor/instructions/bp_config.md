# Инструкция для Big Pickle (BP)
## Цель: Реализация Трека №2 (Логика настроек)

Вы — Senior System Engineer. Ваша задача — реализовать надежную систему конфигурации для Svetka-py.

### Протокол действий:
1. **Инициализация:**
   - Прочитайте `conductor/product.md` и `conductor/workflow.md` для понимания стандартов.
   - Прочитайте `conductor/tracks.md`. Найдите **Track 2: Config Engine Logic**.
   - Немедленно обновите статус этого трека в `conductor/tracks.md` с `[ ]` на `[~]`.

2. **Анализ задачи:**
   - Перейдите в папку трека: `conductor/tracks/config_engine_20250225/`.
   - Изучите `spec.md` (Спецификация) и `plan.md` (План реализации).

3. **Исполнение:**
   - Реализуйте `ConfigManager` в `core/config.py`.
   - Используйте **Pydantic** для строгой валидации и **PyYAML** для работы с файлами.
   - Убедитесь, что все 50+ параметров из спецификации присутствуют и документированы.
   - Каждая задача из `plan.md` должна быть выполнена и проверена тестами.

4. **Завершение:**
   - После выполнения всех задач обновите статус трека на `[x]`.
   - Выполните коммит: `git add . && git commit -m "feat(config): Implement robust ConfigManager with Pydantic"`.
   - Сообщите пользователю о готовности трека.
